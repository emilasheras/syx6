
# Project Deliverable Tasks

- [x] Submit the deliverable via a link to the last commit in the GitHub repository named "PreEntrega1+[Surname]".

## Components to be included:
- [x] `NavBar` component:
  - [x] Create a `NavBar.jsx` file inside a `components` folder within the `src` directory.
  - [x] The `NavBar` should render a navigation bar with a brand and a list of clickable categories.
  - [x] (Optional) Integrate a style library like Bootstrap/Materialize.

- [x] `CartWidget` component:
  - [x] Create a `CartWidget` with an icon and a hardcoded notification number inside the Navbar.

- [x] `ItemListContainer` component:
  - [x] Create an `ItemListContainer.jsx` component with a `greeting` prop.
  - [x] Display a message within the `ItemListContainer` with styling.
  - [x] Import `ItemListContainer` into `App.js` below `NavBar.jsx`.

## Styling:
- [x] Apply styles using Bootstrap/Materialize or another preferred library.
